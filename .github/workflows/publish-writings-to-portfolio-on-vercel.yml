name: Trigger portfolio rebuild

on:
  push:
    branches: [main]
    paths:
      - '/writings/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel rebuild
        run: |
          curl --fail --silent --show-error -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"